# v9.50 Telegram Bot — Python requirements
# For non-Docker deployment (pip install -r requirements.txt)

# Core
z3-solver>=4.16.0
gensim>=4.4.0
scikit-learn>=1.3.0
numpy>=1.24.0
cryptography>=41.0.0
websockets>=12.0

# ML (CPU-only)
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.35.0
tokenizers>=0.15.0
sentence-transformers>=2.2.0

# Note: z-ai-web-dev-sdk is installed via npm (Node.js)
# Install Node.js first: https://nodejs.org
# Then: npm install -g z-ai-web-dev-sdk
